import 'package:flutter/material.dart';

class CameraTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Camera tab page"),
    );
  }
}
