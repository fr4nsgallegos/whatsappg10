import 'package:flutter/material.dart';

class CallsTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Calls tab page"),
    );
  }
}
