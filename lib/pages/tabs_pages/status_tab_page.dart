import 'package:flutter/material.dart';

class StatusTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Status tab page"),
    );
  }
}
